// astro.config.mjs
import { defineConfig } from 'astro/config';

export default defineConfig({
  // The base path for your site if you’re using a subdirectory in GitHub Pages
  base: "/SigmarronWebsite",  // Set this to your repository name
});